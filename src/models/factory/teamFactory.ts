import logger from '@utils/logger'

export default class TeamFactory {
    /**
     * Get event by id
     */
    public static async getMembers() {
        logger.info('team factory method')
    }
}
